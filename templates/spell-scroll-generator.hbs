<section class="ml-app ml-app-shell ml-craftworks app-shell ml-craftworks-spell-scroll-generator">
  <header class="ml-craftworks-header">
    <div>
      <h2>Spell Scroll Generator</h2>
      <p>Choose how many scrolls of each spell level to generate from enabled D&amp;D5e compendium sources. Each draw is random and duplicates are allowed.</p>
    </div>
  </header>

  {{#if hasAccess}}
    {{#unless hasResult}}
    <section class="ml-craftworks-panel">
      <fieldset class="ml-craftworks-generator-constraints">
        <legend>Schools of Magic <small>(all selected by default)</small></legend>
        <div class="ml-craftworks-generator-option-grid">
          {{#each schools}}
            <label><input type="checkbox" data-spell-school="{{id}}" {{#if selected}}checked{{/if}}> {{label}}</label>
          {{/each}}
        </div>
      </fieldset>
      <div class="ml-craftworks-generator-count-grid">
        {{#each levels}}
          <label class="ml-craftworks-generator-count-row">
            <span><strong>{{label}}</strong><small>{{available}} available</small></span>
            <input type="number" min="0" step="1" value="{{count}}" data-scroll-level="{{level}}">
          </label>
        {{/each}}
      </div>
      <div class="ml-actions ml-craftworks-actions">
        <button type="button" data-action="generate-scrolls"><i class="fa-solid fa-dice"></i> Generate</button>
      </div>
    </section>
    {{/unless}}

    {{#if hasResult}}
      <section class="ml-craftworks-panel ml-craftworks-generator-preview">
        <h3><i class="fa-solid fa-scroll"></i> {{resultCount}} Generated Spell Scrolls</h3>
        <div class="ml-craftworks-generator-results">
          {{#each resultGroups}}
            <div class="ml-craftworks-generator-result-group">
              <strong>{{label}}</strong>
              <div class="ml-craftworks-generator-result-list">
                {{#each spells}}
                  <div class="ml-craftworks-generator-result-row" data-document-uuid="{{uuid}}">
                    <img src="{{img}}" alt="{{name}}">
                    <span><strong>{{name}}</strong><small>{{sourceLabel}}</small></span>
                    <button type="button" data-spell-uuid="{{uuid}}" title="Open {{name}}"><i class="fa-solid fa-up-right-from-square"></i></button>
                  </div>
                {{/each}}
              </div>
            </div>
          {{/each}}
        </div>
      </section>

      <section class="ml-craftworks-panel">
        <h3>Recipient</h3>
        <label>Recipient
          <select name="recipient">
            <option value="">Choose character or group…</option>
            {{#each actors}}<option value="{{uuid}}" {{#if selected}}selected{{/if}}>{{name}}</option>{{/each}}
          </select>
        </label>
        {{#if partyInfo.valid}}<p class="ml-craftworks-muted">The configured default is {{partyInfo.actorName}}.</p>{{/if}}
        <div class="ml-actions ml-craftworks-actions">
          <button type="button" data-action="reroll-scrolls"><i class="fa-solid fa-arrows-rotate"></i> Reroll</button>
          <button type="button" data-action="award-scrolls"><i class="fa-solid fa-scroll"></i> Award Generated Spell Scrolls</button>
        </div>
      </section>
    {{/if}}
  {{else}}
    <div class="ml-craftworks-panel"><p class="ml-text ml-craftworks-warning" data-tone="premium">Spell Scroll Generator is a premium Craftworks feature.</p></div>
  {{/if}}
</section>
