<section class="mcw app-shell mcw-browser-shell mcw-marketplace-browser">
  <header class="mcw-header">
    <div>
      <h2>Recipes</h2>
      <p>Browse recipes, discover requirements, and mark recipes for Craft.</p>
    </div>
  </header>

  <div class="mcw-marketplace-browser-body">
    <aside class="mcw-filter-rail">
      <div class="mcw-market-filter-title">
        <strong>Filters</strong>
        <button type="button"
                class="mcw-icon-button"
                data-action="clear-recipe-filters"
                title="Clear all filters">
          <i class="fa-solid fa-filter-circle-xmark"></i>
        </button>
      </div>

      <label class="mcw-filter-search">
        <span>Search</span>
        <input type="search"
               name="search"
               value="{{search}}"
               placeholder="Recipe, source, category…">
      </label>

      <div class="mcw-market-filter-mode-legend">
        <span><i class="fa-regular fa-square"></i> Any</span>
        <span><i class="fa-solid fa-square-check"></i> Include</span>
        <span><i class="fa-solid fa-square-minus"></i> Exclude</span>
      </div>

      <section class="mcw-filter-group">
        <h3>Knowledge</h3>
        <div class="mcw-tristate-list">
          {{#each knowledgeFilters}}
            <button type="button"
                    class="mcw-tristate-filter {{#if included}}is-include{{else if excluded}}is-exclude{{/if}}"
                    data-tristate-filter
                    data-filter-group="knowledge"
                    data-filter-value="{{id}}">
              <span>{{label}}</span>
              <i class="fa-solid {{#if included}}fa-plus{{else if excluded}}fa-minus{{else}}fa-circle{{/if}}"></i>
            </button>
          {{/each}}
        </div>
      </section>

      <section class="mcw-filter-group">
        <h3>Content Pack</h3>
        <div class="mcw-tristate-list">
          {{#each packs}}
            <button type="button"
                    class="mcw-tristate-filter {{#if included}}is-include{{else if excluded}}is-exclude{{/if}}"
                    data-tristate-filter
                    data-filter-group="pack"
                    data-filter-value="{{id}}">
              <span>{{label}}</span>
              <small>{{recipeCount}}</small>
              <i class="fa-solid {{#if included}}fa-plus{{else if excluded}}fa-minus{{else}}fa-circle{{/if}}"></i>
            </button>
          {{/each}}
        </div>
      </section>

      <section class="mcw-filter-group">
        <h3>Category</h3>
        <div class="mcw-tristate-list">
          {{#each categories}}
            <button type="button"
                    class="mcw-tristate-filter {{#if included}}is-include{{else if excluded}}is-exclude{{/if}}"
                    data-tristate-filter
                    data-filter-group="category"
                    data-filter-value="{{id}}">
              <span>{{label}}</span>
              <i class="fa-solid {{#if included}}fa-plus{{else if excluded}}fa-minus{{else}}fa-circle{{/if}}"></i>
            </button>
          {{/each}}
        </div>
      </section>

      <section class="mcw-filter-group">
        <h3>Rarity</h3>
        <div class="mcw-tristate-list">
          {{#each ingredientRarities}}
            <button type="button"
                    class="mcw-tristate-filter {{#if included}}is-include{{else if excluded}}is-exclude{{/if}}"
                    data-tristate-filter
                    data-filter-group="rarity"
                    data-filter-value="{{id}}">
              <span>{{label}}</span>
              <small>{{count}}</small>
              <i class="fa-solid {{#if included}}fa-plus{{else if excluded}}fa-minus{{else}}fa-circle{{/if}}"></i>
            </button>
          {{/each}}
        </div>
      </section>

      {{#each ingredientTagGroups}}
      <section class="mcw-filter-group mcw-filter-subsection">
        <h3>{{label}}</h3>
        <div class="mcw-tristate-list">
          {{#each options}}
            <button type="button"
                    class="mcw-tristate-filter {{#if included}}is-include{{else if excluded}}is-exclude{{/if}}"
                    data-tristate-filter
                    data-filter-group="tag"
                    data-filter-value="{{id}}">
              <span>{{label}}</span>
              <i class="fa-solid {{#if included}}fa-plus{{else if excluded}}fa-minus{{else}}fa-circle{{/if}}"></i>
            </button>
          {{/each}}
        </div>
      </section>
      {{/each}}
</aside>

    <main class="mcw-marketplace-results">
      {{#if canManageRecipeVisibility}}
        <div class="mcw-recipe-visibility-toolbar">
          <span>
            <i class="fa-solid fa-circle-question"></i>
            {{unknownRecipeCount}} recipe(s) with unknown ingredients
          </span>
          <div class="mcw-actions">
            <button type="button" data-action="mark-all-recipes-unknown">
              <i class="fa-solid fa-circle-question"></i> Mark Context Unknown
            </button>
            <button type="button" data-action="mark-all-recipes-known">
              <i class="fa-solid fa-circle-check"></i> Mark Context Known
            </button>
          </div>
        </div>
      {{/if}}

      <div class="mcw-market-results-heading">
        <h2>Recipes</h2>
        <span>{{prospectiveCount}} matches</span>
      </div>

      {{#if overDisplayLimit}}
        <div class="mcw-market-limit-message">
          <i class="fa-solid fa-filter"></i>
          <div>
            <strong>{{prospectiveCount}} recipes match the current filters.</strong>
            <span>
              Showing the first <strong>{{displayLimit}}</strong>. Filter totals continue
              to reflect all available matching recipes.
            </span>
          </div>
        </div>
      {{/if}}
  <div class="mcw-panel mcw-recipe-mark-context">
    <i class="fa-solid fa-bookmark"></i>
    {{#if canMarkRecipes}}
      <span>
        Marked recipes will appear in <strong>{{crafterName}}'s Craft</strong> workspace.
      </span>
    {{else}}
      <span>
        Select a character token, assign a User Character, or own a single character
        to mark recipes for crafting.
      </span>
    {{/if}}
  </div>

  <div class="mcw-reference-list mcw-browser-results">
      {{#if hasRecipes}}
        {{#each recipeGroups}}
          <section class="mcw-recipe-category-group">
            <h3 class="mcw-recipe-category-heading">{{label}}</h3>
            <div class="mcw-recipe-category-list">
              {{#each recipes}}
                <article class="mcw-recipe-card">
                  <header class="mcw-recipe-header">
                    <div class="mcw-recipe-heading">
                      <div class="mcw-recipe-title-line">
                        <h3>{{name}}</h3>

                        <div class="mcw-recipe-title-actions">
                          {{#if ../../canManageRecipeVisibility}}
                            <button type="button"
                                    class="mcw-recipe-visibility-toggle {{#if isUnknown}}mcw-recipe-hidden{{/if}}"
                                    data-recipe-knowledge="{{knowledgeAction}}"
                                    data-recipe-id="{{id}}"
                                    title="{{#if isUnknown}}Ingredients are unknown to players{{else}}Ingredients are known to players{{/if}}">
                              {{#if isUnknown}}
                                <i class="fa-solid fa-circle-question"></i> Unknown
                              {{else}}
                                <i class="fa-solid fa-circle-check"></i> Known
                              {{/if}}
                            </button>
                          {{/if}}

                          <button type="button"
                                  class="mcw-recipe-mark-toggle {{#if isMarked}}mcw-recipe-marked{{/if}}"
                                  data-action="toggle-mark-recipe"
                                  data-recipe-id="{{id}}"
                                  {{#unless canMark}}disabled{{/unless}}>
                            <i class="fa-{{#if isMarked}}solid{{else}}regular{{/if}} fa-bookmark"></i>
                            {{markActionLabel}}
                          </button>
                        </div>
                      </div>

                      <div class="mcw-recipe-meta">
                        {{#if showPremiumBadge}}
                          <span class="mcw-source-badge">{{sourceBadgeLabel}}</span>
                        {{/if}}
                        <span>{{sourceLabel}}</span>
                      </div>
                    </div>

                    {{#if tags.length}}
                      <div class="mcw-pills mcw-recipe-tags">
                        {{#each tags}}<span>{{this}}</span>{{/each}}
                      </div>
                    {{/if}}
                  </header>

                  {{#if description}}
                    <p class="mcw-recipe-description">{{description}}</p>
                  {{/if}}

                  <div class="mcw-recipe-craft-meta">
                    {{#if craftMeta.tool}}
                      <span>
                        <i class="fa-solid fa-screwdriver-wrench"></i>
                        <strong>Tool:</strong> {{craftMeta.tool}}
                      </span>
                    {{/if}}
                    {{#if craftMeta.check}}
                      <span>
                        <i class="fa-solid fa-dice-d20"></i>
                        <strong>Check:</strong> {{craftMeta.check}}
                        {{#if craftMeta.dc}} · DC {{craftMeta.dc}}{{/if}}
                      </span>
                    {{/if}}
                    {{#if craftMeta.hoursRequired}}
                      <span>
                        <i class="fa-solid fa-clock"></i>
                        {{craftMeta.hoursRequired}} hours
                      </span>
                    {{/if}}
                  </div>

                  <div class="mcw-recipe-grid">
                    <section class="mcw-recipe-block">
                      <h4>Requires</h4>
                      {{#if requirementsKnown}}
                        <div class="mcw-requirement-list mcw-requirement-groups">
                          {{#each requirementGroupRows}}
                          <div class="mcw-requirement-group">
                            <div class="mcw-requirement-group-items">
                              {{#each requirementRows}}
                                {{#if alternativeRows.length}}
                                  <div class="mcw-requirement-alternatives">
                                    {{#each alternativeRows}}
                                      <div class="mcw-requirement-row" data-document-uuid="{{materialUuid}}">
                                        {{#if materialImg}}
                                          {{#if materialId}}
                                            <button type="button"
                                                    class="mcw-material-icon-link"
                                                    data-material-id="{{materialId}}">
                                              <img src="{{materialImg}}" alt="">
                                            </button>
                                          {{else}}
                                            <span class="mcw-material-icon-link mcw-material-icon-display">
                                              <img src="{{materialImg}}" alt="">
                                            </span>
                                          {{/if}}
                                        {{/if}}
                                        <span class="mcw-requirement-copy">
          <strong>{{displayLabel}}</strong>
          {{#if displayRarity}}
            <small class="mcw-requirement-rarity">{{displayRarity}}</small>
          {{/if}}
        </span>
                                        {{#unless @last}}<span class="mcw-requirement-or">OR</span>{{/unless}}
                                      </div>
                                    {{/each}}
                                  </div>
                                {{else}}
                                  <div class="mcw-requirement-row" data-document-uuid="{{materialUuid}}">
                                    {{#if materialImg}}
                                      {{#if materialId}}
                                        <button type="button"
                                                class="mcw-material-icon-link"
                                                data-material-id="{{materialId}}">
                                          <img src="{{materialImg}}" alt="">
                                        </button>
                                      {{else}}
                                        <span class="mcw-material-icon-link mcw-material-icon-display">
                                          <img src="{{materialImg}}" alt="">
                                        </span>
                                      {{/if}}
                                    {{/if}}
                                    <span class="mcw-requirement-copy">
          <strong>{{displayLabel}}</strong>
          {{#if displayRarity}}
            <small class="mcw-requirement-rarity">{{displayRarity}}</small>
          {{/if}}
        </span>
                                  </div>
                                {{/if}}
                              {{/each}}
                            </div>
                          </div>
                          {{#unless @last}}
                            <div class="mcw-requirement-group-or">OR</div>
                          {{/unless}}
                          {{/each}}
                        </div>
                      {{else}}
                        <div class="mcw-recipe-unknown-requirements">
                          <i class="fa-solid fa-circle-question"></i>
                          <div>
                            <strong>Ingredients Unknown</strong>
                            <span>This recipe is known, but its required ingredients have not been discovered yet.</span>
                          </div>
                        </div>
                      {{/if}}
                    </section>

                    <section class="mcw-recipe-block">
                      <h4>Produces</h4>
                      <div class="mcw-recipe-output-card mcw-recipe-output-compact" data-document-uuid="{{outputDocumentUuid}}">
                        {{#if output.img}}
                          {{#if outputMaterialId}}
                            <button type="button"
                                    class="mcw-material-icon-link mcw-output-icon-link"
                                    data-material-id="{{outputMaterialId}}">
                              <img src="{{output.img}}" alt="">
                            </button>
                          {{else}}
                            {{#if outputDocumentUuid}}
                              <button type="button"
                                      class="mcw-foundry-output-link"
                                      data-output-document-uuid="{{outputDocumentUuid}}">
                                <img class="mcw-recipe-output-icon"
                                     src="{{output.img}}"
                                     alt="">
                              </button>
                            {{else}}
                              <img class="mcw-recipe-output-icon"
                                   src="{{output.img}}"
                                   alt="">
                            {{/if}}
                          {{/if}}
                        {{/if}}
                        <div>
                          <strong>{{output.label}}</strong>
                          <span>×{{output.quantity}}</span>
                        </div>
                      </div>
                    </section>
                  </div>
                </article>
              {{/each}}
            </div>
          </section>
        {{/each}}
      {{else}}
        <div class="mcw-panel">
          <p>No recipes match the current filters.</p>
        </div>
      {{/if}}
  </div>
    </main>
  </div>
</section>
